name: CI/CD

on:
  push:
    branches: [ main ]

jobs:
 deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: 'main'
      - name: Test Connectivity
        run: |
          ping -c 10 google.com
