
name: CI/CD

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: 'main'
      - name: Check Port 80 on google.com
        run: |
          nc -zv 43.231.127.206 80
